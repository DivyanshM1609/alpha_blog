
<nav class="navbar navbar-expand-lg sticky-top navbar-dark bg-dark">
<div class="container-fluid">
    <a class="navbar-brand text-light" href="/">Alpha Blog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <%= link_to 'Articles', articles_path, class:"nav-link text-light active" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Bloggers', users_path, class:"nav-link text-light active" %>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Categories
                </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><%=link_to "All Categories", categories_path, class:"dropdown-item"%></li>
              <% if logged_in? && current_user.admin? %>
                <li><%=link_to "New Categories", new_category_path, class:"dropdown-item"%></li>
              <% end %> 
            </ul>
          </li>
            <% if logged_in? %>
                <% if !current_user.admin?%>
                <li class="nav-item">
                    <%= link_to current_user.username,user_path(current_user), class:"nav-link text-light active" %>
                </li>
                <%else%>
                    <li class="nav-item">
                        <%= link_to "[Admin]",user_path(current_user), class:"nav-link text-light active" %>
                    </li>
                    <li class="nav-item text-light">
                        <%= link_to current_user.username,user_path(current_user), class:"nav-link text-light active" %>
                    </li>
                <%end%>
                </li>
                <li class="nav-item"> 
                    <%= button_to "Logout", logout_path, method: :delete, class:"nav-link btn btn-outline-primary active ml-2", form: {style: 'display:inline-block,border:none,background-color:black'}  %>
                </li>
            <% else %> 
                <li class="nav-item">
                    <%= link_to "Login", login_path, class:"nav-link active text-light" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Signup", signup_path, class:"nav-link active text-light" %> 
                </li>
            <%end%>
        </div>
    </div>
</div>
</nav>
