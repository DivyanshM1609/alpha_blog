
<nav class="navbar navbar-expand-lg navbar-light bg-light">
<div class="container-fluid">
    <a class="navbar-brand" href="/">Alpha Blog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <%= link_to 'Articles', articles_path, class:"nav-link active" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Bloggers', users_path, class:"nav-link active" %>
            </li>
       
            <% if logged_in? %>
                <li class="nav-item">
                    <%= link_to current_user.username,user_path(current_user), class:"nav-link active" %>
                </li>
                <li class="nav-item"> 
                    <%= button_to "Logout", logout_path, method: :delete, class:"nav-link active ml-2", form: {style: 'display:inline-block,border:none,background-color:transparent'}  %>
                </li>
            <% else %> 
                <li class="nav-item">
                    <%= link_to "Login", login_path, class:"nav-link active" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Signup", signup_path, class:"nav-link active" %> 
                </li>
            <%end%>
        </div>
    </div>
</div>
</nav>
